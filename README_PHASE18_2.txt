
Phase 18.2 — Upgrade UX + Logging Pack

Included:
- API: /api/ai/quota (GET)
- API: /api/log (POST)
- QuotaBanner UI
- UpgradeCTA component
- AI rewrite auto-redirect to /upgrade

What this does:
- Shows quota usage to users
- Redirects users to upgrade when free limit is hit
- Adds simple logging endpoint
- Improves conversion UX

How to use:
1. Copy files into your project
2. Import QuotaBanner in builder page
3. git add .
4. git commit -m "Phase 18.2 upgrade UX"
5. git push

Optional:
- Add UpgradeCTA in PDF/export block later
